# MI250 / gfx90a DeepSeek V4 Flash 工程交接总结

> **状态：WIP / 实验分支。** 本分支包含尚未完成生产验收的 gfx90a、EP 和 batched-prefill 实验。不得将其描述为生产可用，也不得默认启用本文标记为错误或未合格的路径。

## 1. 仓库、分支与基线

- 工作目录：`/home/mi250/Code/hipfire`
- 当前分支：`mi250/deepseek`
- 本分支代码快照提交：`1817b51365a3160378151a19bf20db496c36f191`，`wip(gfx90a): adapt DeepSeek V4 paths for MI250`（本文档以其后的独立提交追加）
- 上游 remote：`origin = https://github.com/warpfront/hipfire.git`
- 上游基线：`origin/beta` 的 `e2f7dd1a47be23873d72090ac778e4f5197b7fe2`，`fix(prefill): rebalance singleton tail chunks`
- fork remote：`husrcf = git@github.com:HUSRCF/hipscope.git`
- 跟踪分支：`husrcf/mi250/deepseek`
- 截至 2026-08-14，本交接文档创建前工作树为 clean。后续开发前仍应运行 `git status --short --branch`，不要覆盖其他 agent 的并行修改。

## 2. 硬件、模型与 RAM 常驻

- 目标硬件：AMD Instinct MI250，CDNA2，`gfx90a`，native wave64。
- 本机可使用 8 个 GCD；EP 实验通常使用相邻 GCD 对，以优先走 die/xGMI 邻接链路。并行 A/B 时可分成 4 对，但必须固定 `HIP_VISIBLE_DEVICES`，避免任务互相争抢。
- 主机内存约 1.0 TiB；当前 `/dev/shm` 约 504 GiB。
- 磁盘模型：`/home/mi250/.hipfire/models/deepseek-v4-flash.mq2lloyd`
- RAM 常驻模型：`/dev/shm/deepseek-v4-flash.mq2lloyd`
- 两者精确大小均为 `86,184,307,283` bytes，约 81 GiB（通常口头称 82 GB）。
- RAM 常驻后，每个 rank 的模型 shard 加载约 `26.6-33.6 s`；原磁盘并发加载约 `126-165 s`，加载阶段约提升 4-5 倍。GPU 执行吞吐不因文件位于 tmpfs 自动提升，主要收益是缩短反复启动 A/B 的准备时间。

RAM 常驻命令：

```bash
cp --reflink=auto /home/mi250/.hipfire/models/deepseek-v4-flash.mq2lloyd \
  /dev/shm/deepseek-v4-flash.mq2lloyd
stat -c '%n %s bytes' /dev/shm/deepseek-v4-flash.mq2lloyd
```

重启后 `/dev/shm` 内容会丢失，需要重新复制。所有新 E2E/PP A/B 应优先使用 RAM 路径，并先核对文件大小。

## 3. 已实现模块

### 3.1 gfx90a wave64 基础适配

- 修正/扩展 gfx90a capability 与 dispatch，避免 CDNA2 默认进入 wave32-only WMMA。
- 新增 MQ2-Lloyd wave64 gate/up、down、row-tile、multi-wave 和 batched K4 研究 kernel。
- 新增 compressor、Q8/F16 twin projection、`wo` 等 wave64 探针与 parity 示例。
- 关键源码：`crates/rdna-compute/src/arch_caps.rs`、`gemv.rs`、`gemm.rs`、`kernels.rs`，以及 `kernels/src/*gfx90a.hip`。
- 已修正过一类严重 host launch 问题：源码按一个 wave64 内两个 32-lane half 计算两行时，host 不能仍发 block32 并同时把 grid 除二，否则奇数行漏算。

### 3.2 MQ2-I8DOT

- 增加 MQ2-Lloyd 72 B/G256 到 affine-I8 同体积表示的 host transcoder：4 个 INT8 codeword + FP16 scale/bias + 原 2-bit index stream，仍为 2.25 bpw。
- 增加 gfx90a `v_dot4_i32_i8` kernel，包括 row1/4/8/16、SG4/SG8、PIPE2、unroll2/4、selector/reduction/epilogue ablation 和 scale-only 性能下界探针。
- 增加 layer selector，可只对指定 transformer 层启用，便于逐层 logits 和 E2E A/B。
- 关键文件：`crates/rdna-compute/src/mq2_i8dot.rs`、`examples/bench_mq2_i8dot_gfx90a.rs`、`kernels/src/gemv_mq2g256_lloyd_i8dot_affine*.gfx90a.hip`。
- CPU 表示验证显示 affine 码本扰动很小，bias correction 恒等式正确；但没有原始 BF16 权重，因此不能声称相对原始模型的完整量化质量已经认证。

### 3.3 grouped MFMA MoE

- 新增 `gemm_mq2g256_lloyd_moe_grouped_mfma.gfx90a.hip`，使用 CDNA2 `v_mfma_f32_16x16x16f16` 路径处理 batched routed experts。
- gfx90a opt-in 后 grouped gate 默认阈值为 160；scalar/grouped 可以通过 env 做同形状 A/B。
- 增加 routed gate/up、SwiGLU、down、unscatter/combine 相关实验分发。
- grouped MFMA 在 PP2048 已有明确收益，但 fused unscatter 路径数值错误，不能合入默认路径。

### 3.4 EP overlap 与 batched prefill

- EP 保持 replicated attention/shared expert，仅 routed partial 做 peer all-reduce，之后再做 `hc_ffn_mix`。
- 增加双 gfx90a、按 row chunk 的 routed expert compute 与 peer reduction overlap 实验，相关开关为 `HIPFIRE_GFX90A_EP_OVERLAP`、`HIPFIRE_GFX90A_EP_CHUNK_ROWS`。
- `ep_deepseek4` 增加 `--prefill-tokens`、`--batched-prefill`、`--prefill-batch`，支持精确 PP token 数和分块 batched prefill。
- 增加 `forward_prefill_batch_chunk_ep`：每 rank 运行 batched attention/FFN，rank-local routed 输出 peer all-reduce，shared 输出留在本 rank，再合并。
- batched prefill 最终 logits 已与 serial Lloyd 对齐，但其 KV/cache continuation 尚不正确，见“已知风险”。

### 3.5 探针与问题分类

- 已按三类拆分 logits/耗时问题：EP all-reduce、gfx942 GEMM/GEMV 复用、量化 routed expert kernel。
- 增加或扩展的探针包括：MQ2 parity、I8DOT ablation、Q8/F16 wave64 shape、compressor twin/ring、EP peer chunk overlap、HFQ tensor inspection、分层状态/top-k dump。
- 常用诊断开关：`HIPFIRE_EP_DECODE_TIMING`、`HIPFIRE_EP_DUMP_POS`、`HIPFIRE_EP_DUMP_IDX`、`HIPFIRE_DEEPSEEK4_DUMP_STATE`、`HIPFIRE_DEEPSEEK4_F32_TRACE`。

## 4. 已验证性能数据

所有数据必须连同 GPU 对、warmup、prompt、batch 和开关保存；下面是当前可用于方向判断的已知结果，不代表稳定生产基准。

### 4.1 Decode，双相邻 GCD，完整 E2E

| 路径 | 吞吐 | 相对 Lloyd baseline |
|---|---:|---:|
| Lloyd baseline | **19.655 tok/s** | 1.000x |
| MQ2-I8DOT，仅 layers 3-10 | **19.948 tok/s** | +1.49% |
| MQ2-I8DOT，layers 3-42 | **21.146 tok/s** | +7.59% |

另一个相邻 GCD 对测得约 `20.017 tok/s`，说明不同 GCD 对/系统状态存在可见波动。长文本测试中曾测得 steady decode `20.728 tok/s`，但语义输出异常，不能作为质量合格证据。

### 4.2 PP2048，双相邻 GCD，B=256，8 chunks，RAM 模型，`--max 0`

| 路径 | 总时间 | 吞吐 | 结论 |
|---|---:|---:|---|
| scalar routed MoE，`HIPFIRE_DEEPSEEK4_MOE_GROUPED=0` | 119.69 s | **17.11 tok/s** | 参考慢路径 |
| gfx90a grouped MFMA，gate=160 | 105.08 s | **19.49 tok/s** | 比 scalar 快约 12.2% |
| grouped MFMA + fused unscatter | 105.13 s | 约 19.48 tok/s | 无性能收益且 logits 错误 |
| routed MoE off，`HIPFIRE_DEEPSEEK4_MOE=0` | 94.42 s | **21.69 tok/s** | attention/shared 等仍是主瓶颈 |

从 grouped MFMA 与 MoE-off 差值估算，routed MoE + EP 通信约增加 `10.66 s`，即约 `5.21 ms/token`，占 grouped 总时间约 10.1%。因此下一阶段应优先优化 batched attention、compressor/indexer 和普通 HFQ4 projection，不应只继续抠 decode GEMV。

### 4.3 模型加载

- RAM tmpfs shard load：**26.6-33.6 s/rank**。
- 原磁盘并发 shard load：约 **126-165 s/rank**。

## 5. 最新 HFQ4 GEMM A/B

需要特别纠正一个容易误判的基线：本次微基准中 rocBLAS 已成功加载，当前 `B >= 4` 的普通 HFQ4 batched baseline 实际会建立/复用 **FP16 shadow**，随后调用 rocBLAS GEMM；它不是“直接对 4-bit 权重做的自定义 baseline”。因此其显存成本和首次 dequant/shadow 建立成本必须单独核算。

在 gfx90a、`M=4096, K=4096, B=256` 的最新 A/B：

| 实现 | 时间 |
|---|---:|
| baseline：FP16 shadow + rocBLAS | **122.9 us** |
| gfx942 direct MFMA v2 复用到 gfx90a | **1038.1 us** |
| gfx942 direct MFMA v3 复用到 gfx90a | **616.2 us** |
| gfx942 direct MFMA v4 复用到 gfx90a | **6740.0 us** |

结论：gfx942 direct MFMA v2/v3/v4 虽可在 gfx90a 编译/发射，但在该真实形状上均明显失败；v3 仍约慢 5 倍，v4 慢约 55 倍。**v4 不是生产候选**。不要因为使用了 MFMA intrinsic 就把 gfx942 layout/tiling 直接接入 gfx90a 默认分发。需要面向 CDNA2 的 wave64、LDS 容量、bank 行为、occupancy、load/dequant pipeline 重新设计。

## 6. 数值正确性与已知风险

### 已通过或有正面证据

- 新 MQ2 gfx90a parity 在 wave1/2/4 配置中通过，未发现当前 barrier 结构死锁。
- batched EP prefill 的最终 prompt logits 与 serial Lloyd 基线几乎一致；短 prompt 的 top-8/logit 数值可对齐，说明 routed partial all-reduce 与 shared merge 的最终 prefill 数学路径基本正确。
- MQ2-I8DOT CPU affine 表示、bias correction 和局部 A8 dot 公式已验证。

### 未通过，禁止默认启用

1. **batched prefill -> decode continuation 错误**：最终 prefill logits 对齐，但生成首 token 后，后续 decode logits/输出与 serial 路径分叉。最可能是 batched attention 写入的 KV/cache 或位置/commit 状态与 decode 读取协议不一致。修复前只能用 `--max 0` 做 prefill 性能测试，不能称为正常 E2E 推理。
2. **fused unscatter/SwiGLU logits 错误**：`HIPFIRE_DEEPSEEK4_FUSED_UNSCATTER_SILU=1` 对 PP2048 没有收益，且最终 logits 与非 fused 路径显著不同。保持关闭。
3. **gfx942 HFQ4 direct MFMA v2/v3/v4 性能失败**：不得进入 gfx90a 默认 dispatch；尤其 v4 明确淘汰。
4. **质量尚未完整认证**：I8DOT 扩大到 layers 3-42 虽提升 decode，但长输出曾出现语义异常。必须做逐层 logits/KL、长上下文和真实生成质量 A/B。
5. **baseline 显存语义**：FP16 shadow + rocBLAS 很快，但会把对应 HFQ4 权重展开为 FP16。全模型无条件 shadow 可能超 VRAM，不能只看 kernel 微秒数。
6. **wave32 WMMA 误分发风险**：gfx90a 不支持本项目 RDNA wave32-only WMMA intrinsic。所有 F16/HFQ4/Q8 batched dispatch 必须核对 `arch_caps` 和实际 code object，不能仅因源码可编译就认为合法。

## 7. 常用构建、运行与开关

构建：

```bash
cd /home/mi250/Code/hipfire
cargo check -p hipfire-arch-deepseek4
cargo build --release -p hipfire-arch-deepseek4 --example ep_deepseek4
cargo build --release -p rdna-compute --example bench_mq2_i8dot_gfx90a
git diff --check
```

双相邻 GCD、PP2048 grouped MFMA 示例：

```bash
cd /home/mi250/Code/hipfire
HIP_VISIBLE_DEVICES=0,1 \
HIPFIRE_GFX90A_MQ2_MFMA=1 \
HIPFIRE_DEEPSEEK4_MOE_GROUPED=1 \
HIPFIRE_DEEPSEEK4_MOE_GROUPED_GATE=160 \
target/release/examples/ep_deepseek4 \
  --model /dev/shm/deepseek-v4-flash.mq2lloyd \
  --tp 2 --prefill-tokens 2048 --batched-prefill --prefill-batch 256 \
  --max 0 --warmup 2 --prompt "DeepSeek prefill benchmark"
```

scalar MoE 对照：

```bash
HIP_VISIBLE_DEVICES=2,3 \
HIPFIRE_DEEPSEEK4_MOE_GROUPED=0 \
target/release/examples/ep_deepseek4 \
  --model /dev/shm/deepseek-v4-flash.mq2lloyd \
  --tp 2 --prefill-tokens 2048 --batched-prefill --prefill-batch 256 \
  --max 0 --warmup 2 --prompt "DeepSeek prefill benchmark"
```

MoE-off 分层对照：

```bash
HIP_VISIBLE_DEVICES=4,5 \
HIPFIRE_DEEPSEEK4_MOE=0 \
target/release/examples/ep_deepseek4 \
  --model /dev/shm/deepseek-v4-flash.mq2lloyd \
  --tp 2 --prefill-tokens 2048 --batched-prefill --prefill-batch 256 \
  --max 0 --warmup 2 --prompt "DeepSeek prefill benchmark"
```

关键 env：

| 开关 | 用途/状态 |
|---|---|
| `HIPFIRE_GFX90A_MQ2_I8DOT=1` | 启用 gfx90a MQ2-I8DOT |
| `HIPFIRE_GFX90A_MQ2_I8DOT_LAYERS=3-42` | 限定 I8DOT 层范围 |
| `HIPFIRE_GFX90A_MQ2_WAVE64=1` | gfx90a MQ2 wave64 研究路径 |
| `HIPFIRE_GFX90A_MQ2_ROW2=1` | MQ2 row2 路径 |
| `HIPFIRE_GFX90A_MQ2_BATCHED_K4_ROW2=1` | 小 batch K4 wave64 row2 |
| `HIPFIRE_GFX90A_MQ2_MFMA=1` | gfx90a grouped MQ2 MFMA prefill |
| `HIPFIRE_DEEPSEEK4_MOE_GROUPED=0/1` | scalar/grouped MoE A/B |
| `HIPFIRE_DEEPSEEK4_MOE_GROUPED_GATE=N` | grouped 路径 batch 阈值 |
| `HIPFIRE_DEEPSEEK4_MOE=0` | 关闭 routed MoE，仅用于热点隔离 |
| `HIPFIRE_DEEPSEEK4_FUSED_UNSCATTER_SILU=1` | **当前错误，保持关闭** |
| `HIPFIRE_GFX90A_EP_OVERLAP=1` | 双 gfx90a EP compute/reduce overlap |
| `HIPFIRE_GFX90A_EP_CHUNK_ROWS=N` | overlap row chunk，默认约 1024 |
| `HIPFIRE_EP_DECODE_TIMING=1` | EP decode 阶段计时 |
| `HIPFIRE_DEEPSEEK4_SKIP_FFN=1` | 隔离 attention 增长 |
| `HIPFIRE_DEEPSEEK4_MAX_COMPRESS_POS=N` | 限制 compressed-KV 扫描长度 |
| `HIPFIRE_DEEPSEEK4_ATTN_PER_POS=1` | batched attention 与逐 position 诊断 A/B |

并行 A/B 时建议使用 `tmux`，每个 pane 固定一对相邻 GCD，例如 `0,1`、`2,3`、`4,5`、`6,7`。不要在同一 GCD 上重叠运行，也不要把并行结果与单任务结果直接混合比较。

## 8. 下一步优先级

1. **batched prefill 分层阶段探针**：在相同 PP2048/B256、同一 GCD 对、RAM 模型上，逐层记录 embedding、q_lora、kv_joint/RoPE、attention core、compressor/indexer、shared FFN、routed MoE、EP reduction 和 head。先找出 MoE-off 仍需 94.42 s 的主耗时；不要只依赖大块总计。
2. **mixed attention 的串行 compressor/indexer**：模型存在大量串行 attention block。挑选若干代表层而非一次跑完 43 层，分别做普通层/长 reach 层 A/B；检查小矩阵是否走 GEMV、FP16 shadow+rocBLAS 或低效 gather，并评估 q_lora/compressor/indexer 的 batch 合并与 fusion。
3. **重新设计 gfx90a HFQ4 batched GEMM**：以 122.9 us 的 FP16-shadow+rocBLAS 为现实性能参照，但同时核算 shadow VRAM。不要继续微调失败的 gfx942 v4；优先做 CDNA2 专用 K pipeline、compute-order weight layout、LDS/occupancy sweep，并先过 4096x4096 B16/32/64/128/256 parity 与 timing。
4. **修复 KV continuation**：对单 chunk 和多 chunk 分别 dump prefill 最后一层 KV/cache、位置索引、有效长度、ring/latent cache；在第一个 decode token 前后与 serial 路径逐层比较。只有连续多 token logits 对齐后，batched prefill 才能进入 E2E 速度报告。
5. **MoE/EP 后续优化**：保留已有效的 grouped MFMA；继续测 grouped gate 阈值、scatter/offset/unscatter 固定成本、routed compute 与 all-reduce overlap。fused unscatter 必须先修 parity。之后再做 expert 共选图 3/3 平衡和少量 expert replication。
6. **质量门槛**：I8DOT、A8 或 mixed-tier 的任何推广都需 router Top-6 一致性、逐层 logits/KL、短/长生成和至少两对 GCD 的重复 A/B。性能提升不能替代正常推理验证。

## 9. 交接结论

当前分支已经证明：gfx90a wave64 和 MQ2 grouped MFMA 能在 MI250 上跑通，MQ2-I8DOT 对 decode 有可测收益，RAM 常驻显著缩短模型装载；但 batched prefill 的 decode continuation、fused unscatter 和 gfx942 HFQ4 MFMA 复用均未合格。下一位开发者应把重点放在 **batched prefill 分层 roofline、CDNA2 专用 HFQ4 GEMM、串行 compressor/indexer，以及 KV continuation 正确性**，并继续把所有实验保持为 opt-in，直至完成数值和 E2E 验收。
